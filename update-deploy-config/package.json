{
  "devDependencies": {
    "@types/node": "^16.10.2",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.8.3"
  },
  "name": "update-deploy-config",
  "version": "1.0.0",
  "description": "Creates/updates argoDeplo.json from action templlate in repository",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maksim Kopnov",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.5.0",
    "@actions/github": "^5.0.0"
  }
}
