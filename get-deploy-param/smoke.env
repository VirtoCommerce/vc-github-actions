# Fixture for .github/scripts/smoke-load.cjs. KEY=VALUE, # starts a comment.

# Fetches its deploy config over the API before reading any field off it
# (getDeployConfig -> JSON.parse -> deployConfig.artifactKey). Offline the fetch
# fails, JSON.parse throws, and it dereferences undefined -- a precondition
# failure strict mode would misread as drift. Unfixable without a token.
SMOKE_STRICT=0

INPUT_ENVNAME=dev
