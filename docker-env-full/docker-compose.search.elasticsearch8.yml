# Search overlay: Elasticsearch 8.x.
# Selected by the action's `searchProvider: elasticsearch8` input (the default).
# Requires the VirtoCommerce.ElasticSearch8 module in the platform image.
# Always combined with docker-compose.yml and a docker-compose.<db>.yml overlay.
services:

  es:
    image: docker.elastic.co/elasticsearch/elasticsearch:${STACK_VERSION:-8.18.0}
    volumes:
      - esdata01:/usr/share/elasticsearch/data
    ports:
      - ${ES_PORT:-9200}:9200
    networks:
      - virto
    environment:
      - node.name=es
      - cluster.name=${CLUSTER_NAME:-elasticsearch}
      - cluster.initial_master_nodes=es
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
      - bootstrap.memory_lock=true
      - xpack.security.enabled=false
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
      - xpack.license.self_generated.type=${LICENSE:-basic}
      - xpack.ml.use_auto_machine_memory_percent=true
    mem_limit: ${MEM_LIMIT:-1g}
    ulimits:
      memlock:
        soft: -1
        hard: -1
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s http://localhost:${ES_PORT:-9200}",
        ]
      interval: 10s
      timeout: 10s
      retries: 120

  vc-platform-web:
    environment:
      - Search:Provider=ElasticSearch8
      - Search:ElasticSearch8:Server=http://es:${ES_PORT:-9200}
      - Search:ElasticSearch8:User=elastic
      - Search:ElasticSearch8:Key=${ELASTIC_PASSWORD}
      # Carried over from the previous hard-coded config; kept to preserve behaviour.
      - Search:ElasticSearch8:EnableCompatibilityMode=true

volumes:
  esdata01:
    driver: local

networks:
  virto:
